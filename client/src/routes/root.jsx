import { Form } from "../components/Form";

const Root = () => {
    return (
        <div>
            <Form type="register" />
            <Form type="login" />
        </div>
    );
};

export default Root;
